name: LUNA
version: "0.1.0"
author: SA <mxsadegh@gmail.com>
about: Earth's moon, named "Luna" in Latin
args:
    - db:
        short: d
        long: db-url
        value_name: DATABASE-URL
        about: "Sets database url Example:\n
                postgres://postgres@localhost/test\n
                sqlite://a.sqlite\n
                sqlite::memory:\n
                sqlite:data.db\n
                sqlite://data.db\n
                sqlite:///data.db\n	
                sqlite://data.db?mode=ro\n"
        takes_value: true
    # - config:
    #     short: c
    #     long: config
    #     value_name: FILE
    #     about: Sets a custom config file
    #     takes_value: true

    # - s:
    #     short: s
    #     long: script
    #     value_name: domain
    #     about: Runs script against domain
    #     takes_value: true
    # TODO 
    # - url:
    #     value_name: url
    #     about: scan this url
    #     index: 1

    # - verbose:
    #     short: v
    #     multiple_occurrences: true
    #     about: Sets the level of verbosity

subcommands:
    - domain:
        about: Controls domains
        version: "0.1"
        author: SA <mxsadegh@gmail.com>
        args:
            - f:
                short: f
                long: fetch
                value_name: domain
                about: Fetch domain
                takes_value: true
            - a:
                short: a
                long: add
                value_name: domain
                about: Adds domain
                takes_value: true
            - s:
                short: s
                long: script
                value_name: domain
                about: Runs script against domains
                takes_value: true
            
    - subdomain:
        about: Controls subdomains
        version: "0.1"
        author: SA <mxsadegh@gmail.com>
        args:
            - f:
                short: f
                long: fetch
                value_name: subdomain
                about: Fetch subdomain
                takes_value: true
            - a:
                short: a
                long: add
                value_name: subdomain
                about: Adds subdomain
                takes_value: true
    - word:
        about: Controls wordlist
        version: "0.1"
        author: SA <mxsadegh@gmail.com>
        args:
            - f:
                short: f
                long: fetch
                value_name: word
                about: Fetch word
                takes_value: true
            - a:
                short: a
                long: add
                value_name: word
                about: Adds to wordlist
                takes_value: true
